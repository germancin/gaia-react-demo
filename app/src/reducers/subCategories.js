
const initialState = {
    fetching: null,
    term: [],
    heroImg: [],
    initialTiles: [],
    moreTiles: [],
    select: 'select',
};

export const subCategories = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};